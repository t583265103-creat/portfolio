{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Webots wheelchair controller",
      "type": "cppvsdbg",
      "request": "attach",
      "processId": "${command:pickProcess}"
    }
  ]
}